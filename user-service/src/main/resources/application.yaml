server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/users_db
    username: admin
    password: admin

    jpa:
      hibernate:
        ddl-auto: update

      show-sql: true

jwt:
  secret: your-super-secret-key-with-at-least-32-characters

  # 1 hour
  expiration: 3600000