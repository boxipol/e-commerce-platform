server:
  port: 8087

spring:
  kafka:
    bootstrap-servers: localhost:29092

  r2dbc:
    url: r2dbc:postgresql://localhost:5435/inventory_db

  flyway:
    url: jdbc:postgresql://localhost:5435/inventory_db
    user: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}