apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-1-app
  labels:
    app: microservice-1-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: microservice-1-app
  template:
    metadata:
      labels:
        app: microservice-1-app
    spec:
      containers:
        - name: microservice-1-container
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 8081

---

apiVersion: v1
kind: Service
metadata:
  name: microservice-1-app
  labels:
    app: microservice-1-app
spec:
  type: LoadBalancer
  selector:
    app: microservice-1-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081