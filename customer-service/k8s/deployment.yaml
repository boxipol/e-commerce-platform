apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  labels:
    app: customer-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-service
  template:
    metadata:
      labels:
        app: customer-service
    spec:
      containers:
        - name: customer-service-container
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 8082

---

apiVersion: v1
kind: Service
metadata:
  name: customer-service
  labels:
    app: customer-service
spec:
  type: LoadBalancer
  selector:
    app: customer-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082